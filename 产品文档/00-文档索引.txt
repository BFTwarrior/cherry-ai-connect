# Cherry AI Connect 文档索引

更新时间：2026-09-24（UTC+8）

## 当前结论

- 当前 GitHub Latest：v1.36，Release、tag 和四个资产已远端回读；v1.34 故障安装包已退出公开发布入口，v1.31 没有公开 Release/安装包，历史 tag、提交和版本记录保留。
- 代码包版本与云同步协议版本独立：当前产品 v1.36，云同步 `schemaVersion` 仍为 1。
- v1.36 类型检查、44 项自动化测试、渲染器构建、Windows x64 安装包和 Web Demo 构建通过；远端附件大小、SHA-256 和下载入口已核实。目标 Windows 设备更新、数据保留和实际同步待现场验收。
- 统计历史分页在缓存裁剪后的旧偏移边界已于本地工作树补修并通过测试，但未包含在已发布 v1.36 安装包；是否纳入后续版本及设备实测仍待完成。
- 最新整理成果：`整理项目/2026-09-24-v1.36发布与验收/`，简明阅读指南配合详细逻辑图 `逻辑图/数据保护与同步验收_1.36-发布与待验收.xmind`；v1.35 与 v1.34 资料分别保留在原项目目录。

## 按用途阅读

1. **规则与风险：** `流程与规范/10-版本开发、测试与GitHub发布流程.txt` → `严重问题核查文档.txt` → `流程与规范/03-测试与验收.txt`。
2. **当前使用：** `01-使用说明.txt`；源码仓库简介与下载链接见仓库根目录 `README.md`。
3. **当前现场待办：** `流程与规范/05-待验收与后续计划.txt`；v1.36 两项修复、发布证据和本地验收见 `../文档/v1.36-请求策略与同步冲突修复.txt`；v1.35 发布证据见 `版本记录/19-1.35启动恢复与数据目录保护.txt`。
4. **协议与架构：** `../sync/SYNC-PROTOCOL.md`；详细产品/同步/界面逻辑图见整理项目目录。
5. **历史验收：** `版本记录/09`、`11`–`17` 及 `1.23`、`1.33` 对应子目录。其结论只描述记录中的历史版本，不替代当前现场验收。

## 资料分类

- `流程与规范/`：跨版本持续有效的工作、开发和验收规则。
- `版本记录/`：按版本冻结的目标、结果、哈希和未完成验收证据；不得把历史版本记录改写成当前状态。
- `1.23/`、`1.33/`、`1.34/`：对应版本的专项说明、计划及逻辑图；后续改动另存版本副本。
- 根目录 `云同步逻辑图.xmind`、`软件界面逻辑图.xmind`：既有原始总图；整理后的 v1.34 统一版另存于整理项目，不覆盖原图。
- 根目录 `1.23*逻辑图.xmind` 与 `1.33/逻辑图/`：历史版本图，按历史归档阅读。

## 不可破坏的约束

- v1.35 正式运行数据位于程序目录同级的 `<程序目录名>-data/`；旧版 `data/` 与恢复备份也必须保留，更新或排错不得清空或用旧数据静默覆盖。
- 同一设备更新、重启、同步不得刷新客户端 API Key；只有全新设备首次恢复可生成本机 Key。
- GitHub 仅使用用户私有同步仓库；用量独立同步，敏感配置加密；不得上传令牌、密码、明文 Key 或真实数据。
- XMind 文件只在本机读取、修改和验证；统一同级主题长度保持关闭，修改后自动清空批注；历史原图默认保留。
