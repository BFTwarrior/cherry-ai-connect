# Cherry AI Connect 1.00 实现现状与验收报告

报告时间：2026-09-16（UTC+8）

## 结论

1.00 源码、自动化测试、Windows 安装包和 GitHub Release 已完成。自动化检查通过；真实 GitHub、真实中转线路、Cherry Studio 和干净 Windows 环境仍需按测试说明现场验收，不能提前写成“全部通过”。

## 已实现

- Electron + React + TypeScript + Vite 桌面应用。
- 中转线路：增删改、启停、连通性测试、模型刷新。
- 客户端 Key：生成、复制地址/Key、编辑、停用、删除；一 Key 只绑定一条线路。
- 名称同步：默认跟随线路名称，手动改名后锁定。
- 思考强度：`low / medium / high / xhigh / max`。
- 本地 API、实时请求、Token/缓存统计、24 小时至半年统计、累计总量和折线图。
- 50 MiB 详细缓存上限；清理旧记录不影响累计统计和配置。
- GitHub 私有仓库加密同步、更新检查、中英文界面、托盘、开机自启、启动隐藏。
- 网关重置：换随机端口、刷新本地 API 地址、停止旧监听、清理残留。

## 路径与发布包

安装包：`C:\Users\BFTwarrior\Desktop\codex\Cherry多线路网关桌面版\dist\Cherry-AI-Connect-Setup-1.00.exe`

运行数据：安装目录旁的 `data`，不要误删。源码在 `electron/ gateway/ renderer/ sync/`，测试在 `tests/`，安装包在 `dist/`。

SHA-256：`842F78771926370A614C97F47E00C785ADB5255D0AA36BDF4DEFAAA6227591EC`

大小：112,329,032 bytes。GitHub Release 资产已发布，网页下载大小一致。

## 检查结果

- 自动化测试：20 项通过。
- 构建、依赖安全扫描、明文凭据扫描：通过。
- 清洁构建可生成 Windows x64 NSIS 安装包。
- 浏览器视觉检查已覆盖页面布局和前端交互。

## 仍需现场验收

1. GitHub Token 首次同步、恢复、过期/撤销、权限错误、换 Token、断网和冲突。
2. 真实中转线路和 Cherry Studio 请求，核对模型、五档思考强度、Token、缓存和错误提示。
3. Windows 安装、覆盖安装、卸载、托盘、开机自启、启动隐藏、关闭到托盘。
4. 50 MiB 清理不删除累计统计、线路和客户端 Key。

401/403/404 分别优先检查上游凭证、权限/额度、仓库或模型名称；不要用删除 `data` 代替修复。
