# Cherry AI Connect v1.35 启动恢复与数据目录保护

状态：构建与隔离打包启动检查通过；目标设备现场验收待完成。

## 目标与范围

- 修复 v1.34 在已消费、已修复旧恢复指针与当前数据缺失同时出现时的主进程未捕获异常。
- 正式版运行数据迁至安装器程序目录的同级独立目录，保留同一安装盘；旧安装目录完整数据仅复制迁移。
- 旧备份恢复由用户在启动保护界面明确选择；提示备份版本与后续本地记录可能缺失，不静默覆盖当前数据。
- 保持现有同步 schema、保险库加解密格式、客户端 Key 与界面其他功能不变。

## 现场依据

- v1.34 目标机安装目录下 `data/` 不存在。
- 恢复指针目标为 1.32，已同时记载首次消费与一次修复时间；旧备份仍含网关关键文件及 `usage.db`。
- 异常从 `restoreUpdateBackupIfNeeded` 传播至 `electron/main.js` 模块顶层，阻断启动。
- 只读检查不等于数据恢复：备份后的新请求是否仍在云端，需另行核对。

## 发布验收

1. `npm run check`、`npm test`、`npm run renderer:build`、Windows x64 安装包及 Web Demo 构建通过。
2. 模拟 v1.34 现场状态，确认不弹未捕获异常；用户退出时原备份与数据不变，明确选择后才恢复。
3. 连续重启、同路径覆盖更新后，独立数据目录、`usage.db`、客户端 Key、GitHub 凭证和用户设置保留。
4. 部分当前数据、无效备份、版本或来源不匹配时不自动覆盖；缺哈希、错误哈希和断网仍安全停止更新。
5. 发布后核对 Release Latest、tag、正文、唯一安装包、blockmap、Web Demo、`latest.yml` 的大小和哈希；目标 Windows 设备实测结果单列。

证据位置：`整理项目/2026-09-23-v1.35启动恢复修复/文档/执行与验收记录.md`，以及本记录后续回填。

## 已获得的证据（2026-09-24）

- `npm run check`、`npm test`（44/44）、`npm run renderer:build`、Windows x64 安装包和 Web Demo 构建均通过。
- 仅读取目标机旧恢复指针与备份元数据：指针目标 1.32，备份检查结果 `available=true`；未写入用户原始数据或指针。
- 隔离启动打包程序时，旧指针不可用于隔离目录，程序显示“本地数据保护”对话框并暂停启动，没有再弹 Electron 未捕获异常。模拟用户同意恢复的代码路径已有单测，目标设备真实恢复未执行。
- 安装包 SHA-256：`EACC7623FE997E4F5CA489FA32386F300AF3290CE4E697FEA653D888A31DBB16`；Web Demo SHA-256：`D2D5B40CA820DC1458D97CA11270F6EE7D80C6C4E44C0241640AF4A986B39C58`。
- 独立复核发现统计库缺失、已消费指针漏补同步凭证和中断复制风险；均已加保护与回归测试。更换安装位置时必须核对旧数据目录，当前不自动搜索未知位置。
- 目标设备的覆盖安装、恢复后连续启动、完整统计记录及真实 GitHub 同步仍待现场验收；不能把上述自动化与隔离检查写成现场通过。
