# 待验收与后续计划

更新时间：2026-09-18（UTC+8）

## 当前事项

1. v1.22 已发布：GitHub API 限流时，更新器读取官方 Release 资产并校验 SHA-256；详见 `../版本记录/09-1.22更新校验问题.txt`。
2. 真实环境：按 `03-测试与验收.txt` 验证 GitHub 同步、客户端 Key、覆盖更新、托盘和真实线路。

## 1.23 预登记

1. `2026-09-18｜1.22/1.21｜GitHub 同步偶发 request timed out，稍后又恢复｜网络/API 超时需要有明确重试和状态说明｜待修复`
2. `2026-09-18｜1.23｜首次同步后中转站 API 不显示，创建一条后全部出现｜首次同步成功后自动显示全部线路、模型和绑定关系｜待修复`
3. 1.23 测试期间出现的其他新问题，按同一格式追加；未获批准不得在 1.23 中修复。

## 记录格式

新问题只登记一次：

`编号｜发现日期（UTC+8）｜版本｜问题｜验收标准｜状态`

状态仅使用：`待确认`、`待修复`、`待验收`、`已完成`。

## 维护提醒

- 界面问题记录页面、窗口尺寸、语言、复现步骤和脱敏截图。
- 同步问题保留本地 `data/`，记录错误码、同步编号和网络状态。
- 发布前再更新 README、Release 说明、下载链接、Latest 标签和哈希；未发布前不得写成“已发布”。
