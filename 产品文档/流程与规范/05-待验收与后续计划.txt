# 待验收与后续计划

更新时间：2026-09-24（UTC+8）

## 当前正式版

- 当前 GitHub Latest：v1.35；本地待发布版本：v1.36。v1.36 源码、类型检查、44 项自动化测试、构建和四个本地产物核对已通过，远端发布尚未完成。v1.34 故障包已撤回公开下载入口。
- 目标 Windows 设备更新至 v1.36 后，设置页排版、同步冲突先选方向再按需输入凭证、原有设置/客户端 Key/统计保留及真实同步尚未现场验收，不得记为全量验收通过。

## 现场验收清单

1. 发布完成后，在原安装路径覆盖更新至 v1.36，启动并连续重启；先核对独立数据目录及必要旧备份，再确认设置、GitHub 凭证、客户端 Key 和历史统计记录保留，不得删除或回滚现有数据。
2. 设置页检查“默认思考强度”说明与选择框左右对齐；打开/关闭下拉菜单时无闪烁、挤压或溢出。
3. 使用既有私有仓库制造同步方向冲突；验证可不输入密码直接选择“保留本机”或“使用云端”，只有实际解锁/解密步骤才按需索取凭证；验证错误/取消/重试不覆盖本机数据。
4. 使用既有私有仓库执行首次/再次同步；核对用量与加密保险库独立、manifest/序列号和本地结果一致、失败可恢复。无需重新输入 token；不得重置客户端 Key。
5. 记录设备/系统、版本、步骤、实际结果和脱敏证据；不得清空 `data/` 或重新生成客户端 Key 来通过测试。

## 历史登记

1.23/1.24 的已登记问题与验收要求属于历史版本档案，见 `../版本记录/11-1.23修复计划.txt`、`../版本记录/12-1.23视觉交互状态设计方案.txt` 及各版本验收记录；不要继续作为当前待办重复执行。发现新问题时按版本发布流程登记，不在本清单夹带修复。
