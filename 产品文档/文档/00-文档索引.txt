# Cherry AI Connect 文档索引

更新时间：2026-09-25（UTC+8）

## 当前结论

- 当前 GitHub Latest：v1.40，Release、tag 和四个资产已发布；v1.34 故障安装包已退出公开发布入口，v1.31 没有公开 Release/安装包，历史 tag、提交和版本记录保留。
- 代码包版本与云同步协议版本独立：当前已发布产品 V1.40.0，网关与界面显示 V1.40，云同步 `schemaVersion` 仍为 1。
- 已发布的 v1.37 基线通过 47/47 项自动化测试；当前 V1.40.0 最终候选实测为 79/79，并通过类型检查、渲染器构建、Web Demo 构建、差异检查和本地 NSIS 打包。现场覆盖更新、数据保留、真实客户端导入、模型拉取、粒子动画和实际同步仍待验收。
- 统计历史分页在缓存裁剪后的旧偏移边界已于当前工作树补修并通过测试；该修复未包含在已发布的 v1.36 安装包，也未形成新的 GitHub Release。
- 当前 V1.40 文档为 `22-v1.40最终版本交付与交叉验证.txt`，详细逻辑图为 `../逻辑图/22-v1.40最终版本交付与交叉验证逻辑图.xmind`；v1.37.1 与 v1.37 导入记录继续保留，v1.36、v1.35 与 v1.34 资料分别保留在历史目录。
- v1.36 是历史公开版本，不能再标为 GitHub Latest；V1.40 已成为当前公开 Latest。V1.40 新增更新切页、官方 Codex 统计来源、同步并发/安全、Cherry Studio 线路诊断、2xx 用量边界和安装包体积门禁修复。
- V1.40.0 安装包为 96,010,081 bytes，已低于 100,000,000 bytes 门槛；发布后仍需完成目标设备现场验收，不得把自动化通过等同于真机验收完成。

## 按用途阅读

1. **规则与风险：** `流程与规范/10-版本开发、测试与GitHub发布流程.txt` → `严重问题核查文档.txt` → `流程与规范/03-测试与验收.txt`。
2. **当前使用：** `01-使用说明.txt`；源码仓库简介与下载链接见仓库根目录 `README.md`。
3. **当前现场待办：** `流程与规范/05-待验收与后续计划.txt`；V1.40 交付与交叉验证见 `22-v1.40最终版本交付与交叉验证.txt`，详细逻辑图见 `../逻辑图/22-v1.40最终版本交付与交叉验证逻辑图.xmind`；v1.37 简报为 `20-v1.37客户端导入与回归修复候选.txt`；v1.36 两项修复、发布证据和本地验收见 `../历史/文档/v1.36-请求策略与同步冲突修复.txt`；v1.35 发布证据见 `../历史/文档/版本记录/19-1.35启动恢复与数据目录保护.txt`。
4. **协议与架构：** `../../sync/SYNC-PROTOCOL.md`；详细产品/同步/界面逻辑图见 `../逻辑图/`。
5. **历史验收：** `../历史/文档/版本记录/09`、`11`–`19` 及 `../历史/文档/1.33`。其结论只描述记录中的历史版本，不替代当前现场验收。

## 资料分类

- `流程与规范/`：跨版本持续有效的工作、开发和验收规则。
- `../历史/文档/版本记录/`：按版本冻结的目标、结果、哈希和未完成验收证据；不得把历史版本记录改写成当前状态。
- `../历史/文档/1.33/`、`../历史/文档/1.34/`：对应版本的专项说明、计划及逻辑图；后续改动另存版本副本。
- 根目录 `云同步逻辑图.xmind`、`软件界面逻辑图.xmind`：既有原始总图；V1.40 交付逻辑图另存为 `22-v1.40最终版本交付与交叉验证逻辑图.xmind`，不覆盖原图；v1.37.1 分区核查图继续保留。
- 根目录 `1.23*逻辑图.xmind` 与 `1.33/逻辑图/`：历史版本图，按历史归档阅读。

## 不可破坏的约束

- v1.35 正式运行数据位于程序目录同级的 `<程序目录名>-data/`；旧版 `data/` 与恢复备份也必须保留，更新或排错不得清空或用旧数据静默覆盖。
- 同一设备更新、重启、同步不得刷新客户端 API Key；只有全新设备首次恢复可生成本机 Key。
- GitHub 仅使用用户私有同步仓库；用量独立同步，敏感配置加密；不得上传令牌、密码、明文 Key 或真实数据。
- 测试数字必须同时注明版本和范围：v1.36 历史记录、公开 v1.37 基线 47/47、v1.37.1 阶段记录 75/75、当前未发布 V1.40.0 候选 79/79 不得混写；此前 53/53、57/57 只作为阶段性记录保留在历史证据中。
- `Codex 官方` 是产品内的本机来源标签：当前工作树只读 `127.0.0.1:43189` loopback API，不能代替 OpenAI 账户级 `/usage`，也不得与中转站用量直接相加。
- XMind 文件只在本机读取、修改和验证；统一同级主题长度保持关闭，修改后自动清空批注；历史原图默认保留。
