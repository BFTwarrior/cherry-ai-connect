Cherry AI 连接中心 1.00｜开发进度暂停点
更新时间：2026-09-16（UTC+8）
暂停原因：Codex 五小时额度仅剩约 6%，按用户要求在接近 5% 时停止，等待额度刷新。

恢复记录：额度刷新后已继续开发。本文件保留为历史暂停点，不再代表当前状态；
最新实现、测试证据和剩余门槛请查看“1.00-实现现状与验收报告.txt”。暂停时未验证
的路径改造已经完成自动测试、打包版真实路径冒烟和浏览器视觉验收。

一、本轮已经写入磁盘的改动
1. 正式安装版的运行数据统一放到“软件安装目录\data”。
2. 浏览器缓存位置为“软件安装目录\data\browser-cache”。
3. 线路、客户端 Key、使用统计、同步状态和保险箱位于“软件安装目录\data\gateway-data”。
4. 如果软件安装到 D 盘，以上主要数据和缓存也跟随到 D 盘，不固定占用 C 盘。
5. 开发环境的数据位置为项目根目录“.runtime-data”，且已加入 Git 忽略规则。
6. 增加 electron/runtime-paths.js，集中计算路径，避免主进程各处重复拼接。
7. 增加 tests/runtime-paths.test.cjs，验证 D 盘安装和开发环境路径。
8. 设置页增加明确说明：删除整个软件文件夹会一并删除缓存和记录；用户不得误删 data。

二、暂停前验证状态
1. 在增加 runtime-paths.js 之前，TypeScript、语法检查和原有 10 项自动测试全部通过。
2. 最新一次路径重构和界面文字改动已经保存，但尚未重新执行测试与打包；恢复后必须先跑完整检查。
3. 尚未生成可交付的 1.00 安装包，不得把当前状态标记为正式验收通过。

三、额度刷新后第一批工作
1. 先运行 TypeScript、语法和全部自动测试，重点确认新增路径测试被执行。
2. 补齐云端恢复、错误密码不覆盖本机、双设备配置冲突与人工选择测试。
3. 清理文档中仍指向 %APPDATA% 和旧端口范围的内容，统一改成安装目录\data。
4. 编译 1.00 候选安装包，在非 C 盘隔离目录试装并确认 data、browser-cache、gateway-data 的真实落点。
5. 完成网页预览验收、正式安装包冒烟测试、密钥扫描、文档与 SHA-256 后再交付。

四、安全边界
1. data 不只是临时缓存，还包含客户端 Key、线路、永久统计和未同步数据。
2. 用户想完全删除本机痕迹时，可以删除整个软件目录；若还需要历史数据，必须先完成云同步或备份 data。
3. 旧版数据首次迁移采用复制而非直接删除，确认新版稳定后再由用户决定是否清理旧目录。
