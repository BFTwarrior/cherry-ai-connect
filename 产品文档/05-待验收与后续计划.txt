# 待验收与后续计划

更新时间：2026-09-18（UTC+8）

## 当前事项

1. v1.21 待真实验收：首次 GitHub 同步完成后，中转站线路列表自动显示已有线路；详见 `07-1.21问题与小调整.txt`。
2. v1.21 待真实验收：四步配置引导模块已从代码、样式和设置状态中删除；详见 `07-1.21问题与小调整.txt`。
3. v1.21 待真实验收：设置页已改为左右独立内容列，修复语言卡片与安全连接卡片高度不平衡造成的异常留白；详见 `07-1.21问题与小调整.txt`。
4. 真实环境：按 `03-测试与验收.txt` 验证 GitHub 同步、客户端 Key、覆盖更新、托盘和真实线路。

## 记录格式

新问题只登记一次：

`编号｜发现日期（UTC+8）｜版本｜问题｜验收标准｜状态`

状态仅使用：`待确认`、`待修复`、`待验收`、`已完成`。

## 维护提醒

- 界面问题记录页面、窗口尺寸、语言、复现步骤和脱敏截图。
- 同步问题保留本地 `data/`，记录错误码、同步编号和网络状态。
- 发布前再更新 README、Release 说明、下载链接、Latest 标签和哈希；未发布前不得写成“已发布”。
