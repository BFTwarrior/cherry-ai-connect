Cherry 多线路网关桌面版测试记录（历史基线与当前交付）

测试版本：0.4.3（保留 0.4.1/0.4.2 历史基线）
测试平台：Windows x64
测试日期：2026-09-10

说明：本文件保留 0.4.1/0.4.2 历史基线；0.4.3 的复制按钮、密钥保密边界和重置端口实测见“六、0.4.3 客户端 Key 快速复制与重置网关开发回归”。
状态说明：历史章节中的“待验收”保留当时记录；当前状态以《UI小功能测评问题清单.txt》的最新章节和本文件末尾的现场复测补记为准。

一、构建检查
[通过] TypeScript 静态检查：npm run check
[通过] Vite 前端构建：npm run renderer:build
[通过] Node 网关语法检查：node --check gateway.mjs
[记录] npm run dist 首次在 NSIS 阶段因下载依赖 ETIMEDOUT；随后配置 electronDist 使用本机 Electron 目录，electron-builder --dir 和 electron-builder --win nsis 均通过并重新生成安装器
[通过] npm run dist 在加入本机 electronDist 配置后完整通过，不再依赖重新下载 Electron 压缩包
[说明] 为保证离线环境可复现，构建配置跳过 Windows 数字签名，但保留 EXE 图标和版本资源编辑
[通过] 桌面工程结构整理为 electron、gateway、renderer、产品文档

二、接口检查
[通过] GET http://127.0.0.1:<当前端口>/health；最终打包版当前实际监听 http://127.0.0.1:27924/health
[通过] 最终打包版新增 GET http://127.0.0.1:27924/admin/api/status，初始 lastClientRequestStatus=never
[通过] 管理接口可以读取线路、版本和支持的思考强度
[通过] 管理接口可以读取客户端 Key 摘要，不返回真实客户端 Key
[通过] /v1/models 会按客户端 Key 的绑定线路返回模型目录
[通过] 真实上游线路检测：HTTP 200，耗时约 889 ms，返回 6 个模型，状态为 ok
[通过] 上游 API Key 不写入普通 API 响应和测试输出

三、界面检查
[通过] 总览页正常渲染，显示网关状态、线路、模型和 Key 统计
[通过] 线路页显示检测状态、模型数、绑定 Key 数，并提供同步/编辑/删除
[通过] 模型目录按线路分组，以网格卡片显示模型
[通过] 客户端 Key 页提供编辑、停用/启用和永久删除
[通过] 设置页提供思考强度、现有 Key 应用、开机自启、托盘、语言和本地 API 地址；不再重复显示默认线路选择
[通过] 思考强度接口返回且可保存五档：low、medium、high、xhigh、max；逐项验证后恢复测试前的原始值 medium
[通过] 深灰色+紫色视觉主题正常显示
[通过] 端口冲突启动复核：占用 27891 后，打包版自动选择 27981；健康检查 ok=true，版本 0.4.1，1 条线路、6 个模型、1 个客户端 Key 保持不变
[通过] 端口持久化复核：释放冲突后重启，程序复用保存的 27981；健康检查仍为 ok=true。验收结束后已恢复到原始 27891，/health 再次通过
[待桌面视觉工具恢复后复核] 通过桌面点击顶部刷新按钮，记录一次实际 UI Toast、页面端口同步和 Cherry 地址提示；本轮桌面控制接口返回应用列表为空，未把未执行的视觉点击标为通过
[通过] 已知旧版网关/本地代理目录移出运行依赖并送入回收站
[通过] 右侧滚动条隐藏，页面仍可滚动
[通过] electron/assets/tray.png 和 electron/assets/app.ico 已生成并打入最终包；托盘、窗口和 EXE 使用同源图形
[需现场确认] Windows 通知区域是否被系统折叠；若被折叠需在任务栏设置中允许 Cherry Gateway 显示

[通过] 临时隔离线路回归：模型同步、客户端 Key 绑定一条线路、XHIGH 写入转发请求、上游 Key 转发和成功状态记录均通过
[通过] 临时隔离异常回归：连接拒绝返回 502 并记录 ECONNREFUSED 可读原因；线路恢复后状态为 ok 且 lastError 清空
[通过] 最终包内部内容核对：app.asar 包含 admin/api/status、复制失败反馈和 reasoning-control；最终 EXE：dist\Cherry-多线路网关-0.4.1.exe
[待验收] UI-001 至 UI-009 已完成代码修复，仍需测试人员按视觉说明逐项点击验收；桌面视觉控制本轮不可用，未虚报视觉点击结果

四、需要持续验证的外部条件
1. 真实聊天请求还依赖中转站网络、代理软件和上游额度。
2. 不同上游对 reasoning.effort、reasoning_effort、xhigh 和 max 的支持可能不同。
3. 正式发布前需要再做一次全新安装环境测试。

安全说明：本文件不记录任何真实 API Key。

======================================================================
当前补充、0.4.3 UI-017 至 UI-024 修复回归
======================================================================

测试日期：2026-09-10

[通过] UI-017：旧版正式 Key 只显示不可点击的“旧版 Key”，不再提供必然失败的“复制 Key”；临时新版 Key 显示复制按钮并成功出现“客户端 Key 已复制”。
[通过] UI-018：窗口缩到约 1048×695 后，线路页编辑/删除操作、设置页单列内容和模型目录同步入口均可见，未发现横向裁切。
[通过] UI-019：总览 QUICK START 已完成的同步项只导航到模型目录，不再隐式重复同步。
[通过] UI-020：线路页移除重复同步入口；模型目录集中提供“同步全部”和各线路“同步”。
[通过] UI-021：总览欢迎区高度、装饰亮度和内边距收紧，辅助说明可读性提高。
[通过] UI-022：批量应用思考等级显示实际 Key 数量，并弹出覆盖范围确认；取消后正式 Key 未改变。
[通过] UI-023：中文模式主要标题和区块标题已统一为中文。
[通过] UI-024：删除无含义满格状态条，改为最近转发结果、重启状态和上游 Key 本机保存提示。

[通过] 重置前端口 27922，确认后随机切换为 27954；新端口 /health 返回 200、ok=true、版本 0.4.3，2 条线路、7 个模型、1 个正式客户端 Key 保留；旧端口连接失败。
[通过] 重置后使用临时客户端 Key 访问新端口 /v1/models 返回 200；临时 Key 已删除，最终只剩 1 个正式 Key。
[待用户验收] UI-017 至 UI-024：本轮已完成现场复核；旧版 Key 入口、窄窗口布局、QUICK START、同步入口、批量确认、中文切换和状态区均已实际检查。UI-017 的新版 Key 完整复制仍待创建临时凭证后复核；新增 UI-025 为低高度侧栏版本信息裁切，状态为待修复。
[待用户验收] UI-001 至 UI-016：保持原标签，不由本轮开发回归代替用户进行托盘、Cherry Studio 完整对话和最终视觉确认。

安全说明：本节不记录任何真实 API Key。

六、0.4.3 客户端 Key 快速复制与重置网关开发回归

1. 构建
[通过] `npm run check`
[通过] `npm run renderer:build`
[通过] `node --check gateway/gateway.mjs`
[通过] `npm run dist`；生成 `C:\Users\BFTwarrior\Desktop\codex\Cherry多线路网关桌面版\dist\Cherry-多线路网关-0.4.3.exe`

2. 客户端 Key 卡片
[通过] 新建临时客户端 Key 后，卡片中显示“复制 Key”和“复制地址”快捷按钮，按钮位于卡片第一行的脱敏摘要区域，不再占用底部生命周期操作区。
[通过] 点击“复制 Key”显示“客户端 Key 已复制”；实际密钥只在本机内存和剪贴板流程中使用，本记录不写入完整值。
[通过] 点击“复制地址”显示“本地 API 地址已复制”；复制值跟随当前实时端口并包含 `/v1`。
[通过] 临时 Key 已绑定 4Router 真实CDK 和 HIGH；测试结束后已永久删除，正式 `Cherry 客户端` 保留。
[通过] 旧版正式 Key 没有可恢复的本地密文时，点击复制显示旧版提示，并提供“重新生成”；本轮没有轮换正式 Key，避免破坏用户当前 Cherry 配置。

3. 保密边界
[通过] 客户端 Key 列表只返回脱敏信息和 `hasSecret`，不返回上游 API Key。
[通过] 上游 API Key 仍由本机网关加密保存并只用于转发；客户端复制接口是本地管理接口，不向中转站发请求。
[说明] 本地客户端 Key 默认只对同一台电脑的 `127.0.0.1` 网关有效；如果同一台电脑已被完全控制，仍需停用、删除或重新生成 Key，不能把本地回环限制当成对本机攻击的绝对防护。

4. 重置网关
[通过] 重置前端口为 `27938`；确认提示明确说明旧 API 地址失效、端口随机切换、线路/模型/客户端 Key 保留。
[通过] 确认后端口切换为 `27902`，顶部和左下角同步显示新端口，状态恢复为“网关在线”。
[通过] 新端口 `/health` 返回 `ok=true`、版本 `0.4.3`；2 条线路、7 个模型、1 个正式客户端 Key 和 `lastClientRequestStatus=ok` 保留。
[通过] 旧端口 `27938` 健康检查连接失败，证明旧本地地址已失效；新端口健康检查成功。

5. 本节结论
代码、构建、本机接口和桌面界面回归通过；新增项目在开发回归阶段曾标记为“待验收”，本轮现场复核后按问题清单改为“待用户验收”。清单中的托盘、真实 Cherry Studio 对话和最终视觉体验仍需用户确认，本节不代替用户验收。

五、0.4.2 当前交付回归

1. 交付物
[通过] 版本号统一为 0.4.2，最终 Windows x64 安装包为：
`C:\Users\BFTwarrior\Desktop\codex\Cherry多线路网关桌面版\dist\Cherry-多线路网关-0.4.2.exe`
[通过] 解包运行版本为 `dist\win-unpacked\Cherry 多线路网关.exe`；本轮健康检查实际返回版本 0.4.2。
[通过] 当前测试实例实际监听 `http://127.0.0.1:27932`；端口仅代表本轮测试实例，用户验收时必须以界面实时显示地址为准。

2. 构建与静态检查
[通过] `npm run check`
[通过] `npm run renderer:build`
[通过] `node --check electron/main.js`
[通过] `node --check electron/preload.js`
[通过] `node --check gateway/gateway.mjs`
[通过] `npm run dist`

3. UI-005：真实客户端请求完成状态
[通过] 网关把最近一次客户端请求时间、状态和模型写入配置；成功转发后重启最终打包版，`/admin/api/status` 仍能读取成功状态。
[通过] 临时上游完成客户端 Key 测试和真实 Chat Completions 转发，转发请求包含设定的 `xhigh`，上游认证使用线路 Key。
[结论] 代码和接口回归通过，问题状态为“待验收”；Cherry Studio 内的最终聊天现场仍按测试人员说明执行。

4. UI-007：重置网关与活动连接
[通过] 隔离测试建立 Keep-Alive/活动连接后执行停止，网关主动清理活动 socket，停止耗时约 1ms，并可在新端口重新监听。
[通过] 重启后线路、模型、客户端 Key 和绑定关系保留。
[结论] 后端回归通过，问题状态为“待验收”；顶部按钮、重启中过渡态和新地址提示属于“待用户验收”之外的开发验收项，仍需测试人员现场点击确认。

5. UI-010 至 UI-016
[通过] 总览统计卡只读，QUICK START 每一步只执行一个明确动作；同步模型直接执行，不再只跳转页面。
[通过] 总览显示有效客户端 Key 的实际绑定线路和思考等级，不再显示无法设置的“默认中转站”。
[通过] 设置改为即时生效；“应用到已有 Key”单独执行；不再混用一张保存表单。
[通过] 顶部本地 API 地址支持复制；客户端 Key 卡片支持连接测试；测试结果写入最近客户端请求状态。
[通过] 批量同步显示当前进度、线路名、失败列表和失败重试。
[通过] “重置网关”使用明确文案和影响提示；重置会随机选择新的本地端口，旧 API 地址失效，但不删除线路、模型、Key 或绑定关系。
[结论] UI-010 至 UI-016 均无剩余“待修复”，状态统一为“待验收”。

6. 客户端 Key 生命周期
[通过] 临时 Key 创建成功；停用后访问 `/v1/models` 返回 401；重新启用后恢复；永久删除后不再出现在列表。
[通过] 新建 Key 保存加密的本地密钥字段，客户端仍只看到一次完整 Key；旧配置使用兼容的绑定线路测试路径。

7. 异常线路
[通过] 临时错误线路测试返回 502，线路状态变为 error，卡片记录可读的 `ECONNREFUSED` 原因；测试线路已清理。

8. 未宣称通过的项目
[待用户验收] UI-001、UI-002、UI-003、UI-004、UI-006、UI-008、UI-009：这些条目当前需要测试人员按界面说明实际点击、观察 Toast、托盘和视觉布局；本轮不代替用户确认。
[待用户验收] 托盘图标是否被 Windows 通知区域折叠、关闭窗口进托盘、单击恢复、右键菜单以及 Cherry Studio 完整聊天流程。
[限制] 桌面控制初始化已按约定尝试两次，均返回应用列表为空和 `nodeRepl.fetch request failed`，因此未把未执行的桌面点击写成通过；之后停止重试。

9. 交付结论
0.4.2 已完成所有“待修复”项目的代码修复、构建和可重复接口回归；清单中“待验收”项目等待测试人员执行开发验收，“待用户验收”项目保持原状态，不在本轮代替用户操作。

安全说明：本文件不记录任何真实 API Key。

======================================================================
2026-09-11 00:23:26 +08:00：0.4.3 用户角度现场复测补记
======================================================================

[通过] UI-001/UI-002：顶部思考强度从 HIGH 切到 LOW 后，设置页、右上角和总览指标立即同步；恢复 HIGH 后一致，已有客户端 Key 保持独立等级。
[通过] UI-017（旧版入口）：正式旧版 Key 显示不可点击的“旧版 Key”，不再显示会必然失败的“复制 Key”。新版 Key 完整复制本轮未创建临时凭证复核。
[通过] UI-018/UI-019/UI-020：窗口约 1046×693 时线路管理、模型同步入口和设置单列布局可见；已完成 QUICK START 项只导航到模型目录；线路页不再提供重复同步入口。
[通过] UI-022/UI-023/UI-024：批量应用显示影响数量并提供覆盖确认；中文/英文切换成功；左下角固定满格状态条已移除。
[待修复] UI-025：低高度窗口下侧栏底部版本信息贴边并被裁切。
[通过] 重置网关：端口由 27954 切到 27961，旧端口请求超时，新 `/health` 为 `ok=true`，2 条线路、7 个模型和 1 个客户端 Key 保留。

======================================================================
2026-09-11：0.4.3 二次确认窗口与旧版 Key 入口修复补记
======================================================================

[通过] 源码中的原生 `window.confirm` 已全部替换为应用内确认组件，覆盖重置、删除线路、删除客户端 Key、批量应用思考强度和重新生成 Key。
[通过] 确认组件已加入深灰+紫色主视觉、危险操作红色强调、重置/覆盖琥珀色强调、取消/确认按钮、遮罩取消和 Esc/Enter 键盘行为。
[通过] 客户端 Key 卡片已移除“旧版 Key”锁定标签；没有本机密文的旧版卡片不再出现“复制 Key”，仍保留复制地址、重新生成及底部生命周期操作。
[通过] `npm run check`、`npm run renderer:build`、`npm run dist` 均通过，安装包为 `dist\\Cherry-多线路网关-0.4.3.exe`。
[限制] 桌面视觉接口连续两次返回 `nodeRepl.fetch request failed`，本轮没有把实际弹窗截图和桌面点击写成通过；交给测试人员按《测试人员完整说明》第十四节复核。
[待用户验收] UI-017 至 UI-024 的最终视觉和交互体验仍保持该标签；UI-025 低高度侧栏版本信息裁切仍为待修复。

本节不记录任何真实 API Key。

======================================================================
2026-09-11：0.4.3 英文界面排版重构回归
======================================================================

[通过] 根节点增加 `locale-en` 排版作用域；中文样式未被英文规则覆盖。
[通过] 英文总览的欢迎标题、说明、四张指标卡和 QUICK START 在约 1266×834 窗口中显示完整，长文本自然换行，没有覆盖或省略关键字。
[通过] 英文 Settings 页的 Request policy、Desktop behavior、Interface language、Security & connection 四张卡布局完整，长说明和“Apply HIGH to 2 existing key(s)”按钮未裁切。
[通过] 英文 Models 页的 Model catalog、搜索栏、Sync all、线路分组、匹配模型数量和单组 Sync 入口显示正常，模型按线路分组，没有重新堆成一张卡片。
[通过] English 切换后已恢复为简体中文；本轮没有创建、删除、停用或重新生成客户端 Key。
[通过] `npm run check`、`npm run renderer:build`、`npm run dist`，最终安装包为 `dist\Cherry-多线路网关-0.4.3.exe`。
[待用户验收] 测试人员仍需按完整说明第十五节在自己的显示缩放和窗口尺寸下复核英文排版；UI-025 低高度侧栏版本信息裁切不属于本次英文排版修复。

======================================================================
2026-09-11：文档补全与交付收口检查
======================================================================

[通过] 已建立《00-交付总览与文档索引.txt》，列出产品目标、产品要求、版本说明、使用说明、测试人员执行稿、问题清单、专项补充、测试记录和踩坑记录的阅读顺序。
[通过] 已统一 0.4.3、Windows x64、最终安装包路径、运行数据目录说明和 JSON + AES-256-GCM 存储边界。
[通过] 已在文档中明确五档思考强度：low、medium、high、xhigh、max；并说明全局默认等级与客户端 Key 独立等级的优先关系。
[通过] 已在文档中明确重置网关的完整验收条件：随机本地端口、旧地址失效、新地址健康、线路/模型/Key/最近状态保留。
[通过] 已统一“待修复”“待验收”“待用户验收”的含义，没有把待用户验收项目改写成已验收。
[通过] 已核对所有交付文档不应包含真实 API Key；文档只允许记录脱敏名称、端口、状态码、模型数量和时间。
[边界] UI-025 低高度侧栏版本信息裁切仍为待修复；托盘、Cherry Studio 完整对话和最终视觉体验等待用户验收项目仍按原标签保留。

本节是文档收口记录，不代表新增了一轮桌面点击，也不改变历史测试证据。

======================================================================
2026-09-11：UI-025 修复与最终开发回归
======================================================================

[通过] UI-025 代码修复：渲染层不再使用 680px 强制最小高度；侧栏允许适配实际视口，底部状态卡和版本信息不会因 flex 压缩而被挤出布局。
[通过] 总览视觉收紧：欢迎区最小高度由 238px 调整为 222px，右侧轨道装饰缩小并降低亮度；核心状态和操作仍保留。
[通过] `npm run check`、`npm run renderer:build`、`node --check electron/main.js`、`node --check electron/preload.js`、`node --check gateway/gateway.mjs`。
[通过] `npm run dist`：重新生成 Windows x64 安装包 `dist\\Cherry-多线路网关-0.4.3.exe`。
[通过] 隔离接口回归：线路创建、模型同步、low/medium/high/xhigh/max 五档、客户端 Key 一对一绑定、绑定模型范围、xhigh 请求透传、上游鉴权、停用 401、删除清理和上游 Key 不落明文均通过。
[待验收] UI-025：需测试人员在打包版最低支持窗口高度下确认侧栏状态卡、`Cherry Gateway` 和 `0.4.3` 版本行完整显示；本轮未把桌面视觉接口结果写成通过。
[待用户验收] UI-001 至 UI-024、托盘图标与托盘交互、开机启动、Cherry Studio 完整对话和最终视觉体验继续保持原标签，不由本轮开发回归代替用户确认。

本节不记录任何真实 API Key。
