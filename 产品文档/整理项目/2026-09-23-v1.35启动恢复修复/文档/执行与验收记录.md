# v1.35 启动恢复修复执行与验收

目标：让 v1.34 目标设备的启动错误得到安全恢复，并防止覆盖安装再次清理用户数据。

## 执行过程与修改记录

1. 先核对《严重问题核查文档》、1.31/1.32 恢复记录及目标设备状态；确认 `data/` 缺失、1.32 指针已消费且已修复、备份仍存在。
2. 将正式版运行数据移到程序目录同级；完整旧数据只复制迁移。启动恢复异常进入保护对话框，旧备份需用户明确选择才恢复。
3. 新增同路径迁移、旧指针、部分数据、备份路径和重复启动回归测试。

## 当前状态

- 版本：v1.35 构建完成；GitHub 发布与目标设备复测待核对。
- 自动化：类型检查、44 项测试、渲染器构建、Windows x64 安装包及 Web Demo 均通过；隔离打包启动显示保护对话框而非未捕获异常。
- 独立复核：补齐统计库缺失、云同步凭证缺失、恢复复制中断和指针替换中断的保护；安装路径变化仍需人工核对旧数据位置，不自动猜测。
- 真实旧备份：只读检查可识别 1.32 备份，未对目标用户数据、指针或备份写入。
- 数据：未删除或修改目标设备的原始备份、恢复指针及现有安装文件。

## 后续待办

完成全量检查、构建和隔离环境安装演练；核对 GitHub 发布资产；在目标设备安装 v1.35 后由用户确认是否从旧备份恢复，并核对历史统计和云同步结果。
