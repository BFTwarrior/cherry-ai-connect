# 资料统一整理执行记录

## 目标与范围

将当前有效文档统一到 v1.34 状态，缩短日常入口文档；保留按版本冻结的历史记录；让主要逻辑图更详细、层级一致、可本地验证。此次不改产品代码、不重打包、不发布新版本。

## 核查发现与处理

- 当前发布源：v1.34 GitHub Latest；真实 Windows 覆盖安装与同步现场验收尚未完成。
- 产品版本与同步协议版本分离：当前产品 v1.34，schema 1。
- 旧“待验收与后续计划”仍列 1.23/1.24 事项，已收敛为当前现场验收；详细历史登记保留在版本记录。
- 旧云同步逻辑图写“24 小时保留”，与当前 `sync/sync-engine.mjs` 的最近 4 个已提交代次策略不符；统一副本按代码修正。
- 界面总图的旧用户批注转为明确约束：关闭“统一同级主题长度”；统一副本中清空该批注。
- 原有版本图和总图不覆盖；v1.34 整理版另存本目录。

## 当前状态

- 文档：索引、使用说明、待验收清单、同步协议说明已统一；整理项目内提供当前产品概览、执行记录与图索引。
- 逻辑图：整理项目内创建产品架构总图，并另存云同步、界面工作台、发布验收的 v1.34 版本图。
- 验收：4 张 XMind 图均通过 CLI 结构检查（0 errors、0 warnings）；`git diff --check` 通过，仅有 Git 的 LF/CRLF 提示。
- 保留：所有旧版文档和原始逻辑图均未覆盖；本次没有改产品代码、重打包、提交或发布。
- 未完成核验：只有云同步图显式记录“统一同级主题长度”为 `inactived`；其余 3 张图未显式记录该属性。桌面控制可见应用列表未发现 XMind，CLI 也没有该开关的设置命令，因此不能确认其界面状态，不能把这一项记为已验收。

## 待办

目标 Windows 设备实际覆盖安装后核对启动、`data/`、客户端 Key、统计、GitHub 同步、托盘和更新失败回退；得到现场证据后再更新验收状态。

完成资料整理后，如在已安装 XMind 的设备上打开其余 3 张图，请确认该选项处于关闭；保存后再用 CLI 复核结构。此项是资料展示设置，不影响 Cherry 产品运行。
