# Cherry AI Connect v1.36 请求策略布局与同步冲突选择修复

状态：v1.36 已发布为 GitHub Latest；目标 Windows 设备与真实同步验收待现场完成。仅包含本记录列出的两项修复。

## 范围

1. 设置页“请求策略”：思考强度选择框移回说明文字右侧，修复窄窗口、文字换行和控件对齐；不改其他设置卡片或使用统计菜单。
2. GitHub 同步冲突：先允许用户直接选择“保留本机”或“使用云端”，再按实际解锁/解密需要获取保险库密码或恢复码；选择动作不执行配置覆盖，最终应用仍保留确认步骤。

## 安全边界

- 已解锁的本机保险库保留本机配置不重复询问密码。
- 云端加密配置必须通过现有 vault 凭证校验后才可恢复；没有凭证时保留冲突选择并允许补充后重试。
- 本机保险库未初始化或锁定时，继续保留本机配置仍需要设置或提供本机保险库凭证；选择方向本身不需要密码。
- 不改变同步协议、加密格式、统计数据、客户端 Key、更新流程和其他界面。

## 验收标准

- 请求策略文字在左、选择框在右；中英文及窄窗口下无溢出、遮挡或意外居中。
- 冲突出现后不显示独立的保险库解锁框或密码门槛即可选任一方向；所选方向下只显示该方向按需使用的凭证输入；取消选择可返回。
- 凭证缺失、错误或云端数据校验失败时，本地配置不被替换，所选方向仍可重试。
- 失败后切换方向时不得沿用前一方向的错误信息；在中等宽度窗口下不得挤压或横向溢出。
- 运行类型检查、自动化测试、前端构建及 Codex Security 变更审查；记录实际结果。
- 发布前确认 v1.36 版本元数据与资产一致；发布后核实 Latest、tag、下载、附件大小和 SHA-256。

## 本地验收记录（2026-09-24）

- `npm run check`：通过。
- `npm test`：44/44 通过。
- `npm run renderer:build`：通过。
- `npm run web-demo:build`：通过。
- `npm run dist`：通过，生成 Windows x64 NSIS 安装包。
- `git diff --check`：通过。
- Codex Security：差异扫描已完成，0 个发现，7/7 变更项已核查；报告指出两个审查回执未进入归档目录。本结论仅限本次差异，不代表全仓库审计或目标设备验收。
- 当前没有 React 组件交互测试；界面行为已做源码审阅及构建验证，冲突恢复仍需目标 Windows 设备实测。

## GitHub 发布与远端验收（2026-09-24）

- Release：<https://github.com/BFTwarrior/cherry-ai-connect/releases/tag/v1.36>；公开、非草稿、非预发布，并标记 Latest。
- `v1.36` tag 指向发布提交 `a23ce276e2204b7cb7833907895c78053ba1db97`；发布后文档更新提交 `734373644183637db066898dd1b6367ac1cd6bf7` 已推送至 `main`。
- 远端四个附件名称、大小及 GitHub SHA-256 均与本地一致；四个公开下载端点 HEAD 均返回 HTTP 200。

| 文件 | 字节 | SHA-256 |
|---|---:|---|
| `Cherry-AI-Connect-Setup-1.36.exe` | 103141711 | `cb1187c959653d10154a772a59c8a29083e861ffc258cbb02180cf64b134a089` |
| `Cherry-AI-Connect-Setup-1.36.exe.blockmap` | 107524 | `bc35f93d651899ea6fcb27df3c3fc256e74e50e21b7865a2d4f46a342e7cd909` |
| `Cherry-AI-Connect-Web-Demo-1.36.zip` | 144099 | `ed5b607d5952429cb229bc7b84832fab49cbe52a7eff6fcb9c4945d6d7e627e0` |
| `latest.yml` | 362 | `5028c885012278b7e26d811f08a14cbaa9ca6aaeff6f8bd6fc400cfdc88510eb` |

`latest.yml` 中安装包大小与 SHA-512 和本地安装包一致。上述为 GitHub API 与公开下载端点回读结果。

## 发布资产（发布前本地核验）

| 文件 | 字节 | SHA-256 |
|---|---:|---|
| `Cherry-AI-Connect-Setup-1.36.exe` | 103141711 | `cb1187c959653d10154a772a59c8a29083e861ffc258cbb02180cf64b134a089` |
| `Cherry-AI-Connect-Setup-1.36.exe.blockmap` | 107524 | `bc35f93d651899ea6fcb27df3c3fc256e74e50e21b7865a2d4f46a342e7cd909` |
| `Cherry-AI-Connect-Web-Demo-1.36.zip` | 144099 | `ed5b607d5952429cb229bc7b84832fab49cbe52a7eff6fcb9c4945d6d7e627e0` |
| `latest.yml` | 362 | `5028c885012278b7e26d811f08a14cbaa9ca6aaeff6f8bd6fc400cfdc88510eb` |

`latest.yml` 中安装包大小与 SHA-512 和本地安装包一致；发布后远端 SHA-256 已逐项复核，见上表。

## 发布后记录

- 目标 Windows 设备更新后检查设置排版、同步方向选择与凭证按需使用；确认既有设置、保险库、GitHub 凭证、使用统计、客户端 Key 保留。
- 用既有私有仓库实测同步冲突、取消、错误与重试，并核对同步成功、代次与历史记录；不清空 `data/` 或重置 Key。
- v1.31 当前在 GitHub 只有历史 tag，没有对应 Release 或安装包；tag/提交仍保留，不得宣称已删除或屏蔽源代码 tag。
